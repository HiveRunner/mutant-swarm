language: java
jdk:
  - openjdk8
os:
  - linux
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true --batch-mode --show-version
script:
  - mvn clean test jacoco:report coveralls:report --batch-mode -DrepoToken=${COVERALLS_REPO_TOKEN}
